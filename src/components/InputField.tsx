import React from "react";

const InputField = () => {
  return (
    <form>
      <input type="input" placeholder="Enter a new task" />
      <button>Add</button>
    </form>
  );
};

export default InputField;
